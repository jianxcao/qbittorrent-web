---
description: 国际化 (i18n) 开发规范
globs: 'src/i18n/**/*.{ts,json}'
alwaysApply: false
---

# 国际化 (i18n) 规范

## 项目配置

### 支持的语言

- **zh-CN**: 简体中文（默认）
- **en-US**: 英文

### 配置要求

```typescript
// src/i18n/index.ts
import { createI18n } from 'vue-i18n'
import zhCN from './locales/zh-CN.json'
import enUS from './locales/en-US.json'

const i18n = createI18n({
  legacy: false, // 使用 Composition API 模式
  locale: localStorage.getItem('locale') || 'en-US',
  fallbackLocale: 'en-US',
  messages: {
    'zh-CN': zhCN,
    'en-US': enUS
  }
})

export default i18n
```

## 翻译文件结构

### JSON 文件组织

```json
{
  "common": {
    "ok": "确定",
    "cancel": "取消",
    "delete": "删除",
    "save": "保存"
  },
  "torrent": {
    "list": {
      "title": "种子列表",
      "empty": "暂无种子"
    },
    "actions": {
      "pause": "暂停",
      "resume": "恢复",
      "delete": "删除"
    },
    "state": {
      "downloading": "下载中",
      "uploading": "上传中",
      "paused": "已暂停"
    }
  },
  "settings": {
    "title": "设置",
    "language": "语言",
    "theme": "主题"
  }
}
```

### 按功能模块组织

```
src/i18n/
├── locales/
│   ├── zh-CN.json        # 简体中文
│   └── en-US.json        # 英文
└── index.ts              # i18n 配置
```

## Key 命名规范

### 1. 使用层级结构

```json
{
  // ✅ 清晰的层级结构
  "torrent.list.title": "种子列表",
  "torrent.list.empty": "暂无种子",
  "torrent.actions.pause": "暂停",
  
  // ❌ 避免扁平结构
  "torrentListTitle": "种子列表",
  "torrentListEmpty": "暂无种子"
}
```

### 2. 使用描述性名称

```json
{
  // ✅ 描述性强
  "settings.appearance.theme": "主题",
  "torrent.details.downloadSpeed": "下载速度",
  
  // ❌ 过于简短
  "theme": "主题",
  "speed": "速度"
}
```

### 3. 复用通用词汇

```json
{
  "common": {
    "confirm": "确认",
    "cancel": "取消",
    "delete": "删除",
    "loading": "加载中...",
    "error": "错误",
    "success": "成功"
  }
}
```

## 在代码中使用

### 模板中使用

```vue
<template>
  <!-- ✅ 使用 $t 函数 -->
  <h1>{{ $t('torrent.list.title') }}</h1>
  
  <!-- ✅ 插值 -->
  <p>{{ $t('torrent.download.progress', { percent: 50 }) }}</p>
  
  <!-- ✅ 复数 -->
  <span>{{ $t('torrent.count', downloadingCount) }}</span>
  
  <!-- ✅ 在属性中使用 -->
  <n-button :title="$t('common.delete')">
    {{ $t('common.delete') }}
  </n-button>
</template>
```

### Script 中使用

```vue
<script setup lang="ts">
import { useI18n } from 'vue-i18n'

const { t } = useI18n()

function showSuccessMessage() {
  window.$message?.success(t('torrent.pause.success'))
}

const confirmText = computed(() => t('common.confirm'))
</script>
```

## 翻译最佳实践

### 1. 使用参数化翻译

```json
{
  "torrent.download.progress": "下载进度: {percent}%",
  "torrent.size.info": "大小: {size} / 共 {total}",
  "user.welcome": "欢迎, {username}!"
}
```

```vue
<template>
  <p>{{ $t('torrent.download.progress', { percent: 50 }) }}</p>
  <p>{{ $t('user.welcome', { username: 'John' }) }}</p>
</template>
```

### 2. 处理复数

```json
{
  "en-US": {
    "torrent.count": "no torrents | {n} torrent | {n} torrents"
  },
  "zh-CN": {
    "torrent.count": "{n} 个种子"
  }
}
```

```vue
<template>
  <p>{{ $t('torrent.count', downloadingCount) }}</p>
</template>
```

### 3. 日期和数字格式化

```typescript
import { useI18n } from 'vue-i18n'

const { d, n } = useI18n()

// 日期格式化
const formattedDate = d(new Date(), 'short')

// 数字格式化
const formattedNumber = n(1234567.89, 'decimal')
const formattedCurrency = n(1234.5, 'currency')
```

## 翻译质量保证

### 1. 保持键值同步

```json
// zh-CN.json
{
  "common": {
    "ok": "确定",
    "cancel": "取消"
  }
}

// en-US.json
{
  "common": {
    "ok": "OK",
    "cancel": "Cancel"
  }
}

// ❌ 避免缺失键
// en-US.json
{
  "common": {
    "ok": "OK"
    // ❌ 缺少 cancel
  }
}
```

### 2. 避免翻译空值

```json
{
  // ❌ 错误：空翻译
  "settings.title": "",
  
  // ✅ 正确：提供有效翻译
  "settings.title": "Settings"
}
```

### 3. 提取重复翻译

```json
{
  // ❌ 错误：重复的翻译值
  "torrent.pause": "暂停",
  "download.pause": "暂停",
  "upload.pause": "暂停",
  
  // ✅ 正确：使用公共 key
  "common.pause": "暂停"
}
```

## 翻译检查工具

### 使用 i18n-checker skill

```bash
# 检查翻译文件
# - 查找未使用的翻译 key
# - 查找缺失的翻译
# - 查找不一致的翻译
# - 查找空值翻译
# - 提取重复翻译为公共 key

# 使用 Cursor Agent:
# "请检查 i18n 翻译文件"
```

## 动态语言切换

### Store 管理语言状态

```typescript
// src/store/setting.ts
export const useSettingStore = defineStore('setting', () => {
  const language = ref<'zh-CN' | 'en-US'>('en-US')
  
  function setLanguage(lang: 'zh-CN' | 'en-US') {
    language.value = lang
    localStorage.setItem('locale', lang)
    
    // 更新 i18n
    const i18n = useI18n()
    i18n.locale.value = lang
    
    // 更新 Naive UI 语言
    updateNaiveUILocale(lang)
  }
  
  return {
    language,
    setLanguage
  }
})
```

### 语言切换器组件

```vue
<script setup lang="ts">
import { useSettingStore } from '@/store'

const settingStore = useSettingStore()

const languageOptions = [
  { label: '简体中文', value: 'zh-CN' },
  { label: 'English', value: 'en-US' }
]

function handleLanguageChange(lang: 'zh-CN' | 'en-US') {
  settingStore.setLanguage(lang)
}
</script>

<template>
  <n-select
    :value="settingStore.language"
    :options="languageOptions"
    @update:value="handleLanguageChange"
  />
</template>
```

## 翻译提示

### 1. 保持简洁

```json
{
  // ✅ 简洁明了
  "common.delete": "删除",
  
  // ❌ 过于冗长
  "common.delete": "点击此按钮以删除所选项目"
}
```

### 2. 使用一致的术语

```json
{
  // ✅ 统一使用"种子"
  "torrent.list.title": "种子列表",
  "torrent.add.button": "添加种子",
  
  // ❌ 术语不一致
  "torrent.list.title": "种子列表",
  "torrent.add.button": "添加 Torrent"
}
```

### 3. 考虑上下文

```json
{
  // ✅ 提供上下文信息
  "torrent.delete.confirm.title": "确认删除种子",
  "torrent.delete.confirm.message": "删除后将无法恢复，是否继续？",
  
  // ❌ 缺少上下文
  "confirm": "确认",
  "message": "是否继续？"
}
```
